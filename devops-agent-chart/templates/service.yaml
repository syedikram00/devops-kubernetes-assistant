apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-agent-chart.fullname" . }}
  labels:
    {{- include "devops-agent-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-agent-chart.selectorLabels" . | nindent 4 }}
